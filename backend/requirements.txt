# ==========================================
# Pureyes (清眸) 后端全量 Python 依赖清单
# 注：PyTorch / Torchvision 推荐根据 GPU 类型使用独立 CUDA Wheel 安装
# ==========================================

# Web 框架与数据库
Flask==2.3.3
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
gunicorn==23.0.0

# 身份认证与安全
PyJWT==2.8.0
Werkzeug>=3.0.0
cryptography>=42.0.0

# 视频与图像处理
opencv-python==4.8.1.78
numpy<2.0.0
Pillow>=10.0.0

# 系统配置与工具
PyYAML>=6.0.1
requests>=2.31.0
python-dotenv==1.0.1
filelock>=3.13.0

# ==========================================
# AI 视觉分析引擎核心依赖 (MVA)
# ==========================================

# YOLOv8 目标检测网络
ultralytics>=8.2.0

# ByteTrack 目标追踪工具
supervision>=0.21.0

# ONNX 推理引擎 (用于加载 OSNet 行人重识别权重)
onnxruntime>=1.17.0

# 嵌入式时空特征向量数据库
lancedb>=0.6.0

# 数据校验与结构化解析
pydantic>=2.0.0

# 多模态视觉模型 Tokenizer / 特征提取
transformers>=4.40.0
